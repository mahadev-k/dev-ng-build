import { RandomBgColorDirective } from './random-bg-color.directive';

describe('RandomBgColorDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomBgColorDirective();
    expect(directive).toBeTruthy();
  });
});
